name: "Install Dependencies"
description: "Action to build the project dependencies from the main versions"
inputs:
  python_version:
    required: true
    type: string
    default: "3.12.1"

runs:
  using: composite
  steps:
    - name: Create NirmatAI Finetuning POD
      env:
        POD_NAME: forgejo
      run: make run-dev
      shell: bash